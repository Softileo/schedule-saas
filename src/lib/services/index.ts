/**
 * Services Layer
 *
 * Business logic extracted from components and pages.
 * Services contain pure business logic, independent of UI.
 */

export {
    getDashboardStats,
    getGreeting,
    getFirstName,
    type EmployeeShiftStats,
    type DashboardStats,
} from "./dashboard.service";
